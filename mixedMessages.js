vLib = []
nLib = []
adjLib = []

const vGenerator 

const nGenerator

const adjGenerator



const whatAreYou = () {
    return `You are a ${adjGenerator} ${nGenerator} ${vGenerator} in the ${nGenerator}`
}